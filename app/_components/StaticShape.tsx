'use client'
import { SHAPE_TYPE } from "@/lib/types";

export default function StaticShape({ shape, selected, onSelect }: {
    shape: SHAPE_TYPE,
    selected: boolean,
    onSelect: () => void,
}) {

}